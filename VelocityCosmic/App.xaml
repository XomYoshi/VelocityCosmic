<Application x:Class="VelocityCosmic.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:VelocityCosmic"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <FontFamily x:Key="Inter">
            pack://application:,,,/Resource/fonts/inter-variable.ttf
        </FontFamily>
        <FontFamily x:Key="JetBrainsMono">
            pack://application:,,,/Resource/fonts/jetbrainsmono-regular.ttf#JetBrains Mono
        </FontFamily>
        <FontFamily x:Key="SegoeFluentIcons">
            pack://application:,,,/Resource/fonts/segoe_002520fluent_002520icons.ttf#Segoe Fluent Icons
        </FontFamily>

        <SolidColorBrush x:Key="ScrollBar.Static.Background" Color="Transparent"/>
        <SolidColorBrush x:Key="ScrollBar.MouseOver.Background" Color="#20FFFFFF"/>
        <SolidColorBrush x:Key="ScrollBar.Pressed.Background" Color="#40FFFFFF"/>
        <SolidColorBrush x:Key="ScrollBar.Disabled.Background" Color="Transparent"/>
        <SolidColorBrush x:Key="ScrollBar.Static.Glyph" Color="#B0B0B0"/>
        <SolidColorBrush x:Key="ScrollBar.MouseOver.Glyph" Color="#D0D0D0"/>
        <SolidColorBrush x:Key="ScrollBar.Pressed.Glyph" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="ScrollBar.Disabled.Glyph" Color="#606060"/>
        <SolidColorBrush x:Key="ScrollBar.Static.Thumb" Color="#33FFFFFF"/>
        <SolidColorBrush x:Key="ScrollBar.MouseOver.Thumb" Color="#55FFFFFF"/>
        <SolidColorBrush x:Key="ScrollBar.Pressed.Thumb" Color="#88FFFFFF"/>
        <Color x:Key="TabBackgroundColor">#080808</Color>
        <Color x:Key="TabSelectedBackgroundColor">#101010</Color>
        <Color x:Key="TabBorderBrushColor">Transparent</Color>
        <Color x:Key="TabTopBarColor">#5c5d5e</Color>
        <SolidColorBrush x:Key="TabItemSelectedBackground" Color="#FF2D2D30"/>
        <Color x:Key="TabForegroundColor">#CCCCCC</Color>
        <Color x:Key="TabSelectedForegroundColor">White</Color>
        <Color x:Key="TabCloseButtonForegroundColor">#888</Color>
        <SolidColorBrush x:Key="TabBackgroundBrush" Color="#080808"/>
        <SolidColorBrush x:Key="TabSelectedBackgroundBrush" Color="#0A0A0A"/>
        <SolidColorBrush x:Key="TabBorderBrush" Color="Transparent"/>
        <SolidColorBrush x:Key="TabTopBarBrush" Color="#5c5d5e"/>
        <SolidColorBrush x:Key="TabForegroundBrush" Color="#CCCCCC"/>
        <SolidColorBrush x:Key="TabSelectedForegroundBrush" Color="White"/>
        <SolidColorBrush x:Key="TabCloseButtonForegroundBrush" Color="#888888"/>
        <SolidColorBrush x:Key="TabContextMenuBackground" Color="#232323"/>
        <SolidColorBrush x:Key="TabContextMenuBorder" Color="#3d3d4a"/>
        <Thickness x:Key="TabContextMenuBorderThickness">1</Thickness>
        <CornerRadius x:Key="TabContextMenuCornerRadius">6</CornerRadius>

        <Style x:Key="WindowControlButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Control.Background" Value="Transparent"/>
            <Setter Property="Control.BorderThickness" Value="0"/>
            <Setter Property="FrameworkElement.Width" Value="45"/>
            <Setter Property="FrameworkElement.Height" Value="36"/>
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Border" Background="{TemplateBinding Control.Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="UIElement.IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Border.Background" Value="#33FFFFFF"/>
                            </Trigger>
                            <Trigger Property="ButtonBase.IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Border.Background" Value="#55FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SettingsNavBorderStyle" TargetType="{x:Type Border}">
            <Setter Property="Border.Background" Value="#1A1A1A"/>
            <Setter Property="Border.CornerRadius" Value="10"/>
            <Setter Property="Border.Padding" Value="10,5"/>
            <Setter Property="FrameworkElement.Cursor" Value="Hand"/>
            <Setter Property="UIElement.SnapsToDevicePixels" Value="True"/>
            <Style.Triggers>
                <Trigger Property="UIElement.IsMouseOver" Value="True">
                    <Setter Property="Border.Background" Value="#2A2A2A"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ExplorerTreeListBoxStyle" TargetType="{x:Type ListBoxItem}">
            <Setter Property="FrameworkElement.Margin" Value="0,2,0,2"/>
            <Setter Property="Control.Padding" Value="6,3"/>
            <Setter Property="Control.Background" Value="Transparent"/>
            <Setter Property="Control.Foreground" Value="White"/>
            <Setter Property="Control.FontFamily" Value="Inter"/>
            <Setter Property="Control.FontWeight" Value="Normal"/>
            <Setter Property="Control.FontSize" Value="15"/>
            <Setter Property="FrameworkElement.Height" Value="36"/>
            <Setter Property="FrameworkElement.Cursor" Value="Hand"/>
            <Setter Property="Control.HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Control.VerticalContentAlignment" Value="Center"/>
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBoxItem}">
                        <Border x:Name="Bd" Background="{TemplateBinding Control.Background}" CornerRadius="6" Margin="10,0,10,2">
                            <Grid Margin="8,0,2,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="38"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Image Grid.Column="0" Width="22" Height="22" Margin="0,0,10,0"
                       RenderOptions.BitmapScalingMode="HighQuality" Opacity="0.75" Stretch="Uniform"
                       Source="{Binding Icon}"/>
                                <TextBlock Grid.Column="1" VerticalAlignment="Center" FontFamily="{StaticResource Inter}" FontWeight="Normal"
                           Opacity="0.7" Text="{Binding Name}"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="UIElement.IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Border.Background" Value="#0DFFFFFF"/>
                            </Trigger>
                            <Trigger Property="ListBoxItem.IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Border.Background" Value="#15FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SettingsTreeView" TargetType="{x:Type ListBoxItem}" BasedOn="{StaticResource ExplorerTreeListBoxStyle}"/>

        <Style x:Key="ToggleSwitchStyle" TargetType="{x:Type ToggleButton}">
            <Setter Property="FrameworkElement.Width" Value="44"/>
            <Setter Property="FrameworkElement.Height" Value="20"/>
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                        <Grid>
                            <Border x:Name="SwitchTrack" CornerRadius="10" Background="#33FFFFFF" Width="44" Height="20"/>
                            <Ellipse x:Name="SwitchThumb" Fill="White" Width="16" Height="16" Margin="2,2,0,2"
                       HorizontalAlignment="Left">
                                <UIElement.RenderTransform>
                                    <TranslateTransform x:Name="ThumbTranslate"/>
                                </UIElement.RenderTransform>
                            </Ellipse>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="ToggleButton.IsChecked" Value="True">
                                <TriggerBase.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetName="SwitchTrack"
                                      Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" To="#01933b"
                                      Duration="0:0:0.25">
                                                <ColorAnimation.EasingFunction>
                                                    <QuadraticEase EasingMode="EaseInOut"/>
                                                </ColorAnimation.EasingFunction>
                                            </ColorAnimation>
                                            <DoubleAnimation Storyboard.TargetName="ThumbTranslate" Storyboard.TargetProperty="X" To="24" Duration="0:0:0.25">
                                                <DoubleAnimation.EasingFunction>
                                                    <QuadraticEase EasingMode="EaseInOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                            <DoubleAnimation Storyboard.TargetName="SwitchThumb" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.25">
                                                <DoubleAnimation.EasingFunction>
                                                    <QuadraticEase EasingMode="EaseInOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </TriggerBase.EnterActions>
                                <TriggerBase.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetName="SwitchTrack"
                                      Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" To="#33FFFFFF"
                                      Duration="0:0:0.25">
                                                <ColorAnimation.EasingFunction>
                                                    <QuadraticEase EasingMode="EaseInOut"/>
                                                </ColorAnimation.EasingFunction>
                                            </ColorAnimation>
                                            <DoubleAnimation Storyboard.TargetName="ThumbTranslate" Storyboard.TargetProperty="X" To="0" Duration="0:0:0.25">
                                                <DoubleAnimation.EasingFunction>
                                                    <QuadraticEase EasingMode="EaseInOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                            <DoubleAnimation Storyboard.TargetName="SwitchThumb" Storyboard.TargetProperty="Opacity" To="0.5"
                                       Duration="0:0:0.25">
                                                <DoubleAnimation.EasingFunction>
                                                    <QuadraticEase EasingMode="EaseInOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </TriggerBase.ExitActions>
                            </Trigger>
                            <Trigger Property="UIElement.IsEnabled" Value="False">
                                <Setter TargetName="SwitchTrack" Property="UIElement.Opacity" Value="0.5"/>
                                <Setter TargetName="SwitchThumb" Property="UIElement.Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseButtonStyle" TargetType="{x:Type Button}"
           BasedOn="{StaticResource WindowControlButtonStyle}">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Border" Background="{TemplateBinding Control.Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="UIElement.IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Border.Background" Value="#E81123"/>
                            </Trigger>
                            <Trigger Property="ButtonBase.IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Border.Background" Value="#C50F1F"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RoundedCloseButtonStylr" TargetType="{x:Type Button}"
           BasedOn="{StaticResource WindowControlButtonStyle}">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Border" Background="{TemplateBinding Control.Background}" CornerRadius="0,10,0,0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="UIElement.IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Border.Background" Value="#E81123"/>
                            </Trigger>
                            <Trigger Property="ButtonBase.IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Border.Background" Value="#C50F1F"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SettingsSidebarButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="FrameworkElement.Height" Value="40"/>
            <Setter Property="Control.HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Control.Padding" Value="18,0,0,0"/>
            <Setter Property="Control.Background" Value="Transparent"/>
            <Setter Property="Control.BorderThickness" Value="0"/>
            <Setter Property="Control.Foreground" Value="#BFBFBF"/>
            <Setter Property="Control.FontSize" Value="14"/>
            <Setter Property="Control.FontFamily" Value="Inter"/>
            <Setter Property="FrameworkElement.Cursor" Value="Hand"/>
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Root" Background="{TemplateBinding Control.Background}">
                            <Grid>
                                <Border x:Name="Indicator" Width="3" Background="#3B82F6" HorizontalAlignment="Right" Opacity="0"/>
                                <ContentPresenter VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="UIElement.IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Border.Background" Value="#141414"/>
                                <Setter Property="Control.Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="ButtonBase.IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Border.Background" Value="#1A1A1A"/>
                                <Setter TargetName="Indicator" Property="UIElement.Opacity" Value="1"/>
                                <Setter Property="Control.Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="WebIconButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Control.Background" Value="Transparent"/>
            <Setter Property="Control.BorderThickness" Value="0"/>
            <Setter Property="FrameworkElement.Cursor" Value="Hand"/>
            <Setter Property="UIElement.Opacity" Value="0.5"/>
            <Setter Property="Control.Padding" Value="14,6"/>
            <Setter Property="FrameworkElement.Margin" Value="10,0,10,0"/>
            <Setter Property="Control.FontFamily" Value="Inter"/>
            <Setter Property="Control.FontWeight" Value="SemiBold"/>
            <Setter Property="Control.FontSize" Value="15"/>
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Control.Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.25">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseInOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <ThicknessAnimation Storyboard.TargetProperty="Margin" To="10,-2,10,2" Duration="0:0:0.25">
                                            <ThicknessAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseInOut"/>
                                            </ThicknessAnimation.EasingFunction>
                                        </ThicknessAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity" To="0.5" Duration="0:0:0.25">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseInOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <ThicknessAnimation Storyboard.TargetProperty="Margin" To="10,0,10,0" Duration="0:0:0.25">
                                            <ThicknessAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseInOut"/>
                                            </ThicknessAnimation.EasingFunction>
                                        </ThicknessAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="WebIconWithoutMargin" TargetType="{x:Type Button}">
            <Setter Property="Control.Background" Value="Transparent"/>
            <Setter Property="Control.BorderThickness" Value="0"/>
            <Setter Property="FrameworkElement.Cursor" Value="Hand"/>
            <Setter Property="UIElement.Opacity" Value="0.5"/>
            <Setter Property="Control.Padding" Value="14,6"/>
            <Setter Property="FrameworkElement.Margin" Value="0,0,0,0"/>
            <Setter Property="Control.FontFamily" Value="Inter"/>
            <Setter Property="Control.FontWeight" Value="SemiBold"/>
            <Setter Property="Control.FontSize" Value="15"/>
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Bd" Background="{TemplateBinding Control.Background}" Margin="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.25">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseInOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <ThicknessAnimation Storyboard.TargetName="Bd" Storyboard.TargetProperty="Margin" To="0,-2,10,2" Duration="0:0:0.25">
                                            <ThicknessAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseInOut"/>
                                            </ThicknessAnimation.EasingFunction>
                                        </ThicknessAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity" To="0.5" Duration="0:0:0.25">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseInOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <ThicknessAnimation Storyboard.TargetName="Bd" Storyboard.TargetProperty="Margin" To="0,0,0,0" Duration="0:0:0.25">
                                            <ThicknessAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseInOut"/>
                                            </ThicknessAnimation.EasingFunction>
                                        </ThicknessAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RepeatButtonTransparent" TargetType="{x:Type RepeatButton}">
            <Setter Property="FrameworkElement.OverridesDefaultStyle" Value="true"/>
            <Setter Property="Control.Background" Value="#00FFFFFF"/>
            <Setter Property="UIElement.Focusable" Value="false"/>
            <Setter Property="Control.IsTabStop" Value="false"/>
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RepeatButton}">
                        <Rectangle Fill="{TemplateBinding Control.Background}" Height="{TemplateBinding FrameworkElement.Height}"
                       Width="{TemplateBinding FrameworkElement.Width}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="HoverOpacityButton" TargetType="{x:Type Button}">
            <Setter Property="Control.Background" Value="Transparent"/>
            <Setter Property="Control.BorderThickness" Value="0"/>
            <Setter Property="Control.Padding" Value="0"/>
            <Setter Property="FrameworkElement.Cursor" Value="Hand"/>
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <ContentPresenter x:Name="content" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.5"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="ButtonBase.IsPressed" Value="True">
                                <Setter TargetName="content" Property="UIElement.Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="UIElement.IsMouseOver" Value="True">
                                <Setter TargetName="content" Property="UIElement.Opacity" Value="1"/>
                            </Trigger>
                            <Trigger Property="UIElement.IsEnabled" Value="False">
                                <Setter TargetName="content" Property="UIElement.Opacity" Value="0.3"/>
                                <Setter Property="FrameworkElement.Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ScrollBarButton" TargetType="{x:Type RepeatButton}">
            <Setter Property="Control.HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Control.VerticalContentAlignment" Value="Center"/>
            <Setter Property="Control.Padding" Value="1"/>
            <Setter Property="UIElement.Focusable" Value="false"/>
            <Setter Property="Control.IsTabStop" Value="false"/>
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RepeatButton}">
                        <Border x:Name="border" SnapsToDevicePixels="true"
                    Background="{StaticResource ScrollBar.Static.Background}">
                            <ContentPresenter x:Name="contentPresenter" Focusable="false"
                                HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}"
                                Margin="{TemplateBinding Control.Padding}"
                                SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}"
                                VerticalAlignment="{TemplateBinding Control.VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="UIElement.IsMouseOver" Value="true">
                                <Setter TargetName="border" Value="{StaticResource ScrollBar.MouseOver.Background}"
                        Property="Border.Background"/>
                            </Trigger>
                            <Trigger Property="ButtonBase.IsPressed" Value="true">
                                <Setter TargetName="border" Value="{StaticResource ScrollBar.Pressed.Background}"
                        Property="Border.Background"/>
                            </Trigger>
                            <Trigger Property="UIElement.IsEnabled" Value="false">
                                <Setter TargetName="contentPresenter" Property="UIElement.Opacity" Value="0.56"/>
                                <Setter TargetName="border" Value="{StaticResource ScrollBar.Disabled.Background}"
                        Property="Border.Background"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ScrollBarThumbVertical" TargetType="{x:Type Thumb}">
            <Setter Property="FrameworkElement.OverridesDefaultStyle" Value="true"/>
            <Setter Property="Control.IsTabStop" Value="false"/>
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Border x:Name="rectangle" CornerRadius="0" Height="{TemplateBinding FrameworkElement.Height}"
                    SnapsToDevicePixels="true" Width="{TemplateBinding FrameworkElement.Width}"
                    Background="{StaticResource ScrollBar.Static.Thumb}"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="UIElement.IsMouseOver" Value="true">
                                <Setter TargetName="rectangle" Value="{StaticResource ScrollBar.MouseOver.Thumb}"
                        Property="Border.Background"/>
                            </Trigger>
                            <Trigger Property="Thumb.IsDragging" Value="true">
                                <Setter TargetName="rectangle" Value="{StaticResource ScrollBar.Pressed.Thumb}"
                        Property="Border.Background"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ScrollBarThumbHorizontal" TargetType="{x:Type Thumb}">
            <Setter Property="FrameworkElement.OverridesDefaultStyle" Value="true"/>
            <Setter Property="Control.IsTabStop" Value="false"/>
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Border x:Name="rectangle" CornerRadius="0" Height="{TemplateBinding FrameworkElement.Height}"
                    SnapsToDevicePixels="true" Width="{TemplateBinding FrameworkElement.Width}"
                    Background="{StaticResource ScrollBar.Static.Thumb}"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="UIElement.IsMouseOver" Value="true">
                                <Setter TargetName="rectangle" Value="{StaticResource ScrollBar.MouseOver.Thumb}"
                        Property="Border.Background"/>
                            </Trigger>
                            <Trigger Property="Thumb.IsDragging" Value="true">
                                <Setter TargetName="rectangle" Value="{StaticResource ScrollBar.Pressed.Thumb}"
                        Property="Border.Background"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type ScrollBar}">
            <Setter Property="Stylus.IsPressAndHoldEnabled" Value="false"/>
            <Setter Property="Stylus.IsFlicksEnabled" Value="false"/>
            <Setter Property="Control.Background" Value="{StaticResource ScrollBar.Static.Background}"/>
            <Setter Property="FrameworkElement.Width" Value="8"/>
            <Setter Property="FrameworkElement.MinWidth" Value="8"/>
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid x:Name="Bg" SnapsToDevicePixels="true">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="0.00001*"/>
                            </Grid.RowDefinitions>
                            <Border Background="{TemplateBinding Control.Background}" Grid.Row="0"/>
                            <Track x:Name="PART_Track" IsDirectionReversed="true" IsEnabled="{TemplateBinding UIElement.IsMouseOver}"
                     Grid.Row="0">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="{x:Static ScrollBar.PageUpCommand}" Style="{StaticResource RepeatButtonTransparent}"/>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="{x:Static ScrollBar.PageDownCommand}" Style="{StaticResource RepeatButtonTransparent}"/>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ScrollBarThumbVertical}"/>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsMouseOver, ElementName=PART_LineDownButton}" Value="true"/>
                                    <Condition Binding="{Binding IsPressed, ElementName=PART_LineDownButton}" Value="true"/>
                                </MultiDataTrigger.Conditions>
                            </MultiDataTrigger>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsMouseOver, ElementName=PART_LineUpButton}" Value="true"/>
                                    <Condition Binding="{Binding IsPressed, ElementName=PART_LineUpButton}" Value="true"/>
                                </MultiDataTrigger.Conditions>
                            </MultiDataTrigger>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsMouseOver, ElementName=PART_LineDownButton}" Value="true"/>
                                    <Condition Binding="{Binding IsPressed, ElementName=PART_LineDownButton}" Value="false"/>
                                </MultiDataTrigger.Conditions>
                            </MultiDataTrigger>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsMouseOver, ElementName=PART_LineUpButton}" Value="true"/>
                                    <Condition Binding="{Binding IsPressed, ElementName=PART_LineUpButton}" Value="false"/>
                                </MultiDataTrigger.Conditions>
                            </MultiDataTrigger>
                            <Trigger Property="UIElement.IsEnabled" Value="false"/>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="ScrollBar.Orientation" Value="Horizontal">
                    <Setter Property="FrameworkElement.Width" Value="Auto"/>
                    <Setter Property="FrameworkElement.MinWidth" Value="0"/>
                    <Setter Property="FrameworkElement.Height" Value="8"/>
                    <Setter Property="FrameworkElement.MinHeight" Value="8"/>
                    <Setter Property="Control.Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ScrollBar}">
                                <Grid x:Name="Bg" SnapsToDevicePixels="true">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="0.00001*"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Background="{TemplateBinding Control.Background}" Grid.Column="0"/>
                                    <Track x:Name="PART_Track" Grid.Column="0" IsEnabled="{TemplateBinding UIElement.IsMouseOver}">
                                        <Track.DecreaseRepeatButton>
                                            <RepeatButton Command="{x:Static ScrollBar.PageLeftCommand}" Style="{StaticResource RepeatButtonTransparent}"/>
                                        </Track.DecreaseRepeatButton>
                                        <Track.IncreaseRepeatButton>
                                            <RepeatButton Command="{x:Static ScrollBar.PageRightCommand}" Style="{StaticResource RepeatButtonTransparent}"/>
                                        </Track.IncreaseRepeatButton>
                                        <Track.Thumb>
                                            <Thumb Style="{StaticResource ScrollBarThumbHorizontal}"/>
                                        </Track.Thumb>
                                    </Track>
                                </Grid>
                                <ControlTemplate.Triggers>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding IsMouseOver, ElementName=PART_LineRightButton}" Value="true"/>
                                            <Condition Binding="{Binding IsPressed, ElementName=PART_LineRightButton}" Value="true"/>
                                        </MultiDataTrigger.Conditions>
                                    </MultiDataTrigger>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding IsMouseOver, ElementName=PART_LineLeftButton}" Value="true"/>
                                            <Condition Binding="{Binding IsPressed, ElementName=PART_LineLeftButton}" Value="true"/>
                                        </MultiDataTrigger.Conditions>
                                    </MultiDataTrigger>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding IsMouseOver, ElementName=PART_LineRightButton}" Value="true"/>
                                            <Condition Binding="{Binding IsPressed, ElementName=PART_LineRightButton}" Value="false"/>
                                        </MultiDataTrigger.Conditions>
                                    </MultiDataTrigger>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding IsMouseOver, ElementName=PART_LineLeftButton}" Value="true"/>
                                            <Condition Binding="{Binding IsPressed, ElementName=PART_LineLeftButton}" Value="false"/>
                                        </MultiDataTrigger.Conditions>
                                    </MultiDataTrigger>
                                    <Trigger Property="UIElement.IsEnabled" Value="false"/>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="{x:Type ScrollViewer}">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollViewer}">
                        <Grid x:Name="Grid" Background="{TemplateBinding Control.Background}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Rectangle x:Name="Corner" Grid.Column="1" Grid.Row="1" Fill="{StaticResource ScrollBar.Static.Background}"/>
                            <ScrollContentPresenter x:Name="PART_ScrollContentPresenter"
                                      CanContentScroll="{TemplateBinding ScrollViewer.CanContentScroll}" CanHorizontallyScroll="false"
                                      CanVerticallyScroll="false" ContentTemplate="{TemplateBinding ContentControl.ContentTemplate}"
                                      Content="{TemplateBinding ContentControl.Content}" Grid.Column="0"
                                      Margin="{TemplateBinding Control.Padding}" Grid.Row="0"/>
                            <ScrollBar x:Name="PART_VerticalScrollBar" AutomationProperties.AutomationId="VerticalScrollBar"
                         Cursor="Arrow" Grid.Column="1" Maximum="{TemplateBinding ScrollViewer.ScrollableHeight}"
                         Minimum="0" Grid.Row="0"
                         Visibility="{TemplateBinding ScrollViewer.ComputedVerticalScrollBarVisibility}"
                         ViewportSize="{TemplateBinding ScrollViewer.ViewportHeight}"
                         Value="{Binding VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}"/>
                            <ScrollBar x:Name="PART_HorizontalScrollBar" AutomationProperties.AutomationId="HorizontalScrollBar"
                         Cursor="Arrow" Grid.Column="0" Maximum="{TemplateBinding ScrollViewer.ScrollableWidth}" Minimum="0"
                         Orientation="Horizontal" Grid.Row="1"
                         Visibility="{TemplateBinding ScrollViewer.ComputedHorizontalScrollBarVisibility}"
                         ViewportSize="{TemplateBinding ScrollViewer.ViewportWidth}"
                         Value="{Binding HorizontalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="Tabs" TargetType="{x:Type TabControl}">
            <Setter Property="Control.Padding" Value="2"/>
            <Setter Property="Control.HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Control.VerticalContentAlignment" Value="Center"/>
            <Setter Property="Control.Background" Value="{StaticResource TabItemSelectedBackground}"/>
            <Setter Property="Control.BorderThickness" Value="0"/>
            <Setter Property="Control.Foreground" Value="White"/>
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TabControl}">
                        <Grid x:Name="templateRoot" ClipToBounds="true" SnapsToDevicePixels="true"
                  KeyboardNavigation.TabNavigation="Local">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <ScrollViewer x:Name="TabScrollViewer" VerticalScrollBarVisibility="Disabled"
                            HorizontalScrollBarVisibility="Auto" Background="#00FFFFFF" ClipToBounds="true" Grid.Column="0"
                            Grid.Row="0">
                                <TabPanel x:Name="HeaderPanel" IsItemsHost="True" Background="{x:Null}"/>
                            </ScrollViewer>
                            <Button x:Name="AddTabButton" Grid.Column="1" Width="30" Height="30" Margin="4"
                      HorizontalAlignment="Center" VerticalAlignment="Center" Padding="0"
                      Style="{StaticResource HoverOpacityButton}">
                                <Grid Width="16" Height="16">
                                    <Path Stroke="#FFB9BBBE" StrokeThickness="1" Data="M 0,4.5 L 9,4.5 "/>
                                    <Path Stroke="#FFB9BBBE" StrokeThickness="1" Data="M 4.5,0 L 4.5,9 "/>
                                </Grid>
                            </Button>
                            <Border x:Name="contentPanel" BorderBrush="{TemplateBinding Control.BorderBrush}"
                      BorderThickness="{TemplateBinding Control.BorderThickness}"
                      Background="{TemplateBinding Control.Background}" Grid.ColumnSpan="2" Grid.Row="1"
                      KeyboardNavigation.DirectionalNavigation="Contained" KeyboardNavigation.TabIndex="2"
                      KeyboardNavigation.TabNavigation="Local">
                                <ContentPresenter x:Name="PART_SelectedContentHost" ContentSource="SelectedContent" Margin="0"
                                  SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}"/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SubMenuItemStyle" TargetType="{x:Type MenuItem}">
            <Setter Property="Control.Foreground" Value="#FFFFFF"/>
            <Setter Property="Control.FontFamily" Value="Inter"/>
            <Setter Property="Control.FontSize" Value="14"/>
            <Setter Property="FrameworkElement.Height" Value="34"/>
            <Setter Property="Control.Padding" Value="8,0,20,0"/>
            <Setter Property="Control.Background" Value="Transparent"/>
            <Setter Property="FrameworkElement.Cursor" Value="Hand"/>
            <Setter Property="UIElement.SnapsToDevicePixels" Value="True"/>
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type MenuItem}">
                        <Grid>
                            <Border x:Name="Border" Background="Transparent" CornerRadius="4" SnapsToDevicePixels="True">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter Grid.Column="0" ContentSource="Icon" VerticalAlignment="Center" Margin="6,0,10,0"/>
                                    <ContentPresenter Grid.Column="1" VerticalAlignment="Center" RecognizesAccessKey="True" ContentSource="Header"
                                    TextElement.Foreground="#FFFFFF" Opacity="0.9" Margin="0,0,20,0"/>
                                    <Image Grid.Column="2" Source="/Resource/Exp/chevron-right.png"
                         RenderOptions.BitmapScalingMode="HighQuality" SnapsToDevicePixels="True" Width="12" Height="12"
                         VerticalAlignment="Center" Margin="6,0,6,0">
                                        <FrameworkElement.Style>
                                            <Style TargetType="{x:Type Image}">
                                                <Setter Property="UIElement.Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=MenuItem}, Path=HasItems}"
                                       Value="True">
                                                        <Setter Property="UIElement.Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </FrameworkElement.Style>
                                    </Image>
                                </Grid>
                            </Border>
                            <Popup x:Name="PART_Popup" Focusable="False" AllowsTransparency="True" HorizontalOffset="140"
                     PopupAnimation="Fade" IsOpen="{TemplateBinding MenuItem.IsSubmenuOpen}" VerticalOffset="-40">
                                <Border Background="#222" BorderBrush="#555" BorderThickness="1" CornerRadius="6" Padding="6"
                        Margin="4,0,0,0">
                                    <StackPanel IsItemsHost="True"/>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="MenuItem.IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Border.Background" Value="#33FFFFFF"/>
                            </Trigger>
                            <Trigger Property="UIElement.IsEnabled" Value="False">
                                <Setter Property="Control.Foreground" Value="#777"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="Tab" TargetType="{x:Type TabItem}">
            <Setter Property="FrameworkElement.FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Control.Foreground" Value="{DynamicResource TabForegroundBrush}"/>
            <Setter Property="Control.Background" Value="{DynamicResource TabBackgroundBrush}"/>
            <Setter Property="FrameworkElement.Margin" Value="0"/>
            <Setter Property="Control.Padding" Value="20,4,36,0"/>
            <Setter Property="FrameworkElement.Height" Value="40"/>
            <Setter Property="Control.HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Control.VerticalContentAlignment" Value="Center"/>
            <Setter Property="HeaderedContentControl.HeaderTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock VerticalAlignment="Center" FontSize="13" Opacity="0.8"
                       RenderOptions.BitmapScalingMode="HighQuality" SnapsToDevicePixels="True" Text="{Binding}"
                       Foreground="{Binding RelativeSource={RelativeSource AncestorType=TabItem}, Path=Foreground}"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TabItem}">
                        <Grid x:Name="RootGrid" Background="Transparent" RenderTransformOrigin="0,0" SnapsToDevicePixels="True">
                            <UIElement.RenderTransform>
                                <TranslateTransform X="-50"/>
                            </UIElement.RenderTransform>
                            <Border x:Name="topBlueBar" Background="{DynamicResource TabTopBarBrush}" Height="2"
                      HorizontalAlignment="Stretch" VerticalAlignment="Top" Visibility="Collapsed" Panel.ZIndex="1"
                      SnapsToDevicePixels="True"/>
                            <Border x:Name="mainBorder" Background="{DynamicResource TabBackgroundBrush}" CornerRadius="0"
                      BorderBrush="{DynamicResource TabBorderBrush}" BorderThickness="0,0,1,0" Height="40"
                      SnapsToDevicePixels="True">
                                <Grid x:Name="TabContentWrapper" VerticalAlignment="Center">
                                    <FrameworkElement.LayoutTransform>
                                        <ScaleTransform x:Name="TabScale" ScaleX="1" ScaleY="1"/>
                                    </FrameworkElement.LayoutTransform>
                                    <Grid x:Name="TabContentGrid" VerticalAlignment="Center" SnapsToDevicePixels="True">
                                        <FrameworkElement.ContextMenu>
                                            <ContextMenu x:Name="TabContextMenu" Background="#080808" Placement="Bottom" HorizontalOffset="50"
                                   VerticalOffset="10" BorderBrush="#191919" BorderThickness="2" Padding="4" HasDropShadow="True"
                                   SnapsToDevicePixels="True"
                                   PlacementTarget="{Binding RelativeSource={RelativeSource AncestorType=TabItem}}">
                                                <Control.Template>
                                                    <ControlTemplate TargetType="{x:Type ContextMenu}">
                                                        <Border x:Name="RootBorder" Background="{TemplateBinding Control.Background}"
                                    BorderBrush="{TemplateBinding Control.BorderBrush}"
                                    BorderThickness="{TemplateBinding Control.BorderThickness}" CornerRadius="6"
                                    SnapsToDevicePixels="True" RenderTransformOrigin="0.5,0.0" Opacity="0">
                                                            <UIElement.RenderTransform>
                                                                <TranslateTransform Y="10"/>
                                                            </UIElement.RenderTransform>
                                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle" Margin="2"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <EventTrigger RoutedEvent="ContextMenu.Opened">
                                                                <BeginStoryboard>
                                                                    <Storyboard>
                                                                        <DoubleAnimation Storyboard.TargetName="RootBorder"
                                                     Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="10" To="0"
                                                     Duration="0:0:0.3">
                                                                            <DoubleAnimation.EasingFunction>
                                                                                <CubicEase EasingMode="EaseOut"/>
                                                                            </DoubleAnimation.EasingFunction>
                                                                        </DoubleAnimation>
                                                                        <DoubleAnimation Storyboard.TargetName="RootBorder" Storyboard.TargetProperty="Opacity" From="0" To="1"
                                                     Duration="0:0:0.3"/>
                                                                    </Storyboard>
                                                                </BeginStoryboard>
                                                            </EventTrigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Control.Template>
                                                <FrameworkElement.Resources>
                                                    <Style TargetType="{x:Type MenuItem}">
                                                        <Setter Property="Control.Foreground" Value="#FFFFFF"/>
                                                        <Setter Property="Control.FontFamily" Value="Inter"/>
                                                        <Setter Property="Control.FontSize" Value="14"/>
                                                        <Setter Property="FrameworkElement.Height" Value="34"/>
                                                        <Setter Property="Control.Padding" Value="8,0,20,0"/>
                                                        <Setter Property="Control.Background" Value="Transparent"/>
                                                        <Setter Property="Control.BorderThickness" Value="1"/>
                                                        <Setter Property="Control.BorderBrush" Value="#525252"/>
                                                        <Setter Property="FrameworkElement.Cursor" Value="Hand"/>
                                                        <Setter Property="UIElement.SnapsToDevicePixels" Value="True"/>
                                                        <Setter Property="Control.Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="{x:Type MenuItem}">
                                                                    <Grid>
                                                                        <Border x:Name="Border" Background="Transparent" CornerRadius="4" SnapsToDevicePixels="True">
                                                                            <Grid>
                                                                                <Grid.ColumnDefinitions>
                                                                                    <ColumnDefinition Width="Auto"/>
                                                                                    <ColumnDefinition Width="*"/>
                                                                                    <ColumnDefinition Width="Auto"/>
                                                                                </Grid.ColumnDefinitions>
                                                                                <ContentPresenter Grid.Column="0" ContentSource="Icon" VerticalAlignment="Center" Margin="6,0,10,0"/>
                                                                                <ContentPresenter Grid.Column="1" Margin="0,0,20,0" VerticalAlignment="Center" RecognizesAccessKey="True"
                                                          ContentSource="Header" TextElement.Foreground="#FFFFFF" Opacity="0.9"/>
                                                                                <Image Grid.Column="2" Source="/Resource/Exp/Tabs_chevron-right.png" Opacity="0.8" Width="15" Height="15"
                                               VerticalAlignment="Center" Margin="6,0,6,0">
                                                                                    <FrameworkElement.Style>
                                                                                        <Style TargetType="{x:Type Image}">
                                                                                            <Setter Property="UIElement.Visibility" Value="Collapsed"/>
                                                                                            <Style.Triggers>
                                                                                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=MenuItem}, Path=HasItems}"
                                                             Value="True">
                                                                                                    <Setter Property="UIElement.Visibility" Value="Visible"/>
                                                                                                </DataTrigger>
                                                                                            </Style.Triggers>
                                                                                        </Style>
                                                                                    </FrameworkElement.Style>
                                                                                </Image>
                                                                            </Grid>
                                                                        </Border>
                                                                        <Popup x:Name="PART_Popup" Focusable="False" AllowsTransparency="True" HorizontalOffset="154"
                                           IsOpen="{TemplateBinding MenuItem.IsSubmenuOpen}" VerticalOffset="-40">
                                                                            <Border Background="#222" BorderBrush="{TemplateBinding Control.BorderBrush}"
                                              BorderThickness="{TemplateBinding Control.BorderThickness}" CornerRadius="6" Padding="6"
                                              Margin="4,0,0,0">
                                                                                <StackPanel IsItemsHost="True"/>
                                                                            </Border>
                                                                        </Popup>
                                                                    </Grid>
                                                                    <ControlTemplate.Triggers>
                                                                        <Trigger Property="MenuItem.IsHighlighted" Value="True">
                                                                            <Setter TargetName="Border" Property="Border.Background" Value="#33FFFFFF"/>
                                                                        </Trigger>
                                                                        <Trigger Property="UIElement.IsEnabled" Value="False">
                                                                            <Setter Property="Control.Foreground" Value="#777"/>
                                                                        </Trigger>
                                                                    </ControlTemplate.Triggers>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                </FrameworkElement.Resources>
                                                <MenuItem x:Name="DuplicateMenuItem" Header="Duplicate" Tag="CopyyTab">
                                                    <MenuItem.Icon>
                                                        <Image Source="/Resource/Tabs/Menu/Tab_Copy.png" Width="18" Height="18"
                                   RenderOptions.BitmapScalingMode="HighQuality" SnapsToDevicePixels="True"/>
                                                    </MenuItem.Icon>
                                                </MenuItem>
                                                <MenuItem x:Name="RenameMenuItem" Header="Rename" Tag="RenameTab">
                                                    <MenuItem.Icon>
                                                        <Image Source="/Resource/Tabs/Menu/Tab_Rename.png" Width="18" Height="18"
                                   RenderOptions.BitmapScalingMode="HighQuality" SnapsToDevicePixels="True"/>
                                                    </MenuItem.Icon>
                                                </MenuItem>
                                                <MenuItem x:Name="ExecuteMenuItem" Header="Execute" Tag="ExecuteTab">
                                                    <MenuItem.Icon>
                                                        <Image Source="/Resource/Bt/RunFileExe.png" Width="18" Height="18"
                                   RenderOptions.BitmapScalingMode="HighQuality" SnapsToDevicePixels="True"/>
                                                    </MenuItem.Icon>
                                                </MenuItem>
                                                <MenuItem x:Name="CloseOtherMenuItem" Header="Close all but this" Tag="CloseAllButThis">
                                                    <MenuItem.Icon>
                                                        <Image Source="/Resource/Tabs/Menu/Tab_Close.png" Width="20" Height="20"
                                   RenderOptions.BitmapScalingMode="HighQuality" SnapsToDevicePixels="True"/>
                                                    </MenuItem.Icon>
                                                </MenuItem>
                                            </ContextMenu>
                                        </FrameworkElement.ContextMenu>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Image Width="16" Height="16" Opacity="0.8" Margin="10,0,5,0" Grid.Column="2"
                           RenderOptions.BitmapScalingMode="HighQuality"

                           Source="{Binding RelativeSource={RelativeSource AncestorType=TabItem}, Path=Tag}"/>
                                        <ContentPresenter x:Name="contentPresenter" ContentSource="Header" VerticalAlignment="Center" Margin="0,0,10,0"
                                      Grid.Column="3" Opacity="0.8" TextElement.FontSize="15"
                                      TextElement.FontFamily="{StaticResource Inter}" RenderOptions.BitmapScalingMode="HighQuality"
                                      SnapsToDevicePixels="True"/>
                                        <Button x:Name="CloseButton" Width="16" Height="16" FontSize="10" Margin="6,0,12,0" Grid.Column="4"
                            FontFamily="Segoe MDL2 Assets" Content="" Background="Transparent"
                            Foreground="{DynamicResource TabCloseButtonForegroundBrush}" Cursor="Hand"
                            Style="{StaticResource HoverOpacityButton}" Opacity="0" SnapsToDevicePixels="True"
                            BorderBrush="{x:Null}"/>
                                    </Grid>
                                </Grid>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <EventTrigger RoutedEvent="TabItem.Loaded">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="RootGrid"
                                     Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)" From="-50" To="0"
                                     Duration="0:0:0.25">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                        <DoubleAnimation Storyboard.TargetName="RootGrid" Storyboard.TargetProperty="Opacity" From="0" To="1"
                                     Duration="0:0:0.25"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <Trigger Property="TabItem.IsSelected" Value="True">
                                <Setter TargetName="mainBorder" Value="{DynamicResource TabSelectedBackgroundBrush}"
                        Property="Border.Background"/>
                                <Setter Value="{DynamicResource TabSelectedForegroundBrush}" Property="Control.Foreground"/>
                                <Setter TargetName="topBlueBar" Property="UIElement.Visibility" Value="Visible"/>
                                <Setter TargetName="CloseButton" Property="UIElement.Opacity" Value="1"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="UIElement.IsMouseOver" Value="True"/>
                                    <Condition Property="TabItem.IsSelected" Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="mainBorder" Value="{DynamicResource TabSelectedBackgroundBrush}"
                        Property="Border.Background"/>
                                <Setter Value="{DynamicResource TabSelectedForegroundBrush}" Property="Control.Foreground"/>
                                <Setter TargetName="CloseButton" Property="UIElement.Opacity" Value="0.5"/>
                            </MultiTrigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="TabItem.IsSelected" Value="False"/>
                                    <Condition Property="UIElement.IsMouseOver" Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="mainBorder" Value="{DynamicResource TabBackgroundBrush}" Property="Border.Background"/>
                                <Setter TargetName="topBlueBar" Property="UIElement.Visibility" Value="Collapsed"/>
                                <Setter TargetName="CloseButton" Property="UIElement.Opacity" Value="0"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="{x:Type ToolTip}">
            <Setter Property="Control.Background" Value="#FF080808"/>
            <Setter Property="Control.BorderBrush" Value="#FF191919"/>
            <Setter Property="Control.BorderThickness" Value="1"/>
            <Setter Property="Control.Padding" Value="8,6"/>
            <Setter Property="Control.FontFamily" Value="Inter"/>
            <Setter Property="Control.FontWeight" Value="SemiBold"/>
            <Setter Property="Control.FontSize" Value="14"/>
            <Setter Property="Control.Foreground" Value="#B3FFFFFF"/>
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ToolTip}">
                        <Border Background="{TemplateBinding Control.Background}"
                    BorderBrush="{TemplateBinding Control.BorderBrush}"
                    BorderThickness="{TemplateBinding Control.BorderThickness}" CornerRadius="4"
                    Padding="{TemplateBinding Control.Padding}">
                            <ContentPresenter Margin="0" HorizontalAlignment="Center" VerticalAlignment="Center"
                                Content="{TemplateBinding ContentControl.Content}"
                                ContentTemplate="{TemplateBinding ContentControl.ContentTemplate}" RecognizesAccessKey="True"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
